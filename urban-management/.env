# Spring
SPRING_PROFILES_ACTIVE=dev
APP_NAME=smart-city-api
SERVER_PORT=8080

# Database
DB_HOST=city-report-db
DB_PORT=5432
DB_NAME=city_report_db
DB_USERNAME=postgres
DB_PASSWORD=postgres

# Kafka
KAFKA_BOOTSTRAP_SERVERS=kafka:9092

# JWT
JWT_SECRET=6e0f943aa85cb0ec1c2ef768261abe86

# Cloudinary
CLOUDINARY_CLOUD_NAME=dohsfqs6d
CLOUDINARY_API_KEY=123894699435121
CLOUDINARY_API_SECRET=iUwG2F8x45rHmXBMDSLBcXwoyQg

# Google map
GOOGLE_MAPS_API_KEY=AIzaSyADrSk_OCXk_gyXUnfsnmX-kg0d_d6VTR8


; import { RoleName } from "@/features/role/types";
; import {
;   LayoutDashboard,
;   FileWarning,
;   Users,
;   Settings,
;   BarChart,
;   Tags,
;   MapPin,
;   Building2,
;   Bell,
; } from "lucide-react";
;
; export interface MenuItem {
;   path: string;
;   label: string;
;   icon: any;
;   roles: RoleName[];
; }
;
; export const MENU_CONFIG: MenuItem[] = [
;   {
;     path: "/admin/dashboard",
;     label: "Dashboard",
;     icon: LayoutDashboard,
;     roles: [RoleName.ADMIN, RoleName.STAFF],
;   },
;   {
;     path: "/admin/reports",
;     label: "Reports",
;     icon: FileWarning,
;     roles: [RoleName.ADMIN, RoleName.STAFF],
;   },
;   {
;     path: "/admin/categories",
;     label: "Categories",
;     icon: Tags,
;     roles: [RoleName.ADMIN],
;   },
;   {
;     path: "/admin/map",
;     label: "Map Monitoring",
;     icon: MapPin,
;     roles: [RoleName.ADMIN, RoleName.STAFF],
;   },
;   {
;     path: "/admin/users",
;     label: "Users",
;     icon: Users,
;     roles: [RoleName.ADMIN],
;   },
;   {
;     path: "/admin/departments",
;     label: "Departments",
;     icon: Building2,
;     roles: [RoleName.ADMIN],
;   },
;   {
;     path: "/admin/notifications",
;     label: "Notifications",
;     icon: Bell,
;     roles: [RoleName.ADMIN, RoleName.STAFF],
;   },
;
;   {
;     path: "/admin/analytics",
;     label: "Analytics",
;     icon: BarChart,
;     roles: [RoleName.ADMIN],
;   },
;   {
;     path: "/admin/settings",
;     label: "Settings",
;     icon: Settings,
;     roles: [RoleName.ADMIN, RoleName.STAFF],
;   },
; ];
;
; export const getFilteredMenu = (role: RoleName): MenuItem[] => {
;   return MENU_CONFIG.filter((item) => item.roles.includes(role));
; };
; staff sẽ không có tab report, mà sẽ có "tasks"
; staff sẽ quản lý task được assign tự động, staff sẽ thực hiện quản lý task bằng cách update status task
