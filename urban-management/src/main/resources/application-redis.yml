spring:
  data:
    redis:
#      host: ${REDIS_HOST:localhost}
#      port: ${REDIS_PORT:6379}
      host: localhost
      port: 6379
      timeout: 2s
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0